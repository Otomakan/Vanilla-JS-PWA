const actions = []

const dispatchers = []

const store = []



const createStore = (initialState) => {
  get : function (target, prop, target) {
    
  }
  return new Proxy(initialState, handler)
}


module.exports = {
  createStore
}